{


  del_step = 0.10;
  // del_step = 0.20;
  // del_step = 2.50;
  mixing = kTRUE;
  // del_step = 0.50; // Viktor

  binAnders = kTRUE;
  // rebin anders binning by 2
  binAndersIdx = 2;
  binAndersIdx = 1;
  // if (binAnders) del_step = 0.01*binAndersIdx;

}
